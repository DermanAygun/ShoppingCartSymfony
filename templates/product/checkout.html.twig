{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}

    {{ form_start(checkout) }}

    {{ form_widget(checkout) }}

    {{ form_end(checkout) }}

{% endblock %}